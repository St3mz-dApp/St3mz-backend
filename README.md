# St3mz backend

## Commands

Install dependencies

```bash
yarn
```

Start server listening to changes in source code:

```bash
yarn dev
```

Build project:

```bash
yarn build
```

Start server for production:

```bash
yarn start
```
